apiVersion: 1

providers:
  - name: TraceFox
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: ${TRACEFOX_DASHBOARD_PATH}
      foldersFromFilesStructure: false
